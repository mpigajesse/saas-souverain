# secrets — jamais de clé en clair (règle AGENTS.md n°6)
tls/
*.pem
*.key
*.crt
wg_private*

# environnement local
.venv/
__pycache__/
node_modules/

# artefacts de test/build
coverage.out
cov-prod.out
*.test
/tmp/

# binaires compilés (go build) — ne jamais committer
/testcontainers
/wgmesh
orchestrator-go/wgmesh
.ci/testcontainers/testcontainers

# génération de documents (scripts ReportLab/Graphviz) — local uniquement
docs/*
!docs/bench/
docs/bench/*
!docs/bench/baseline.json

# PDF — jamais poussés (documents générés + notes locales)
*.pdf

# agents IA / skills opencode — local uniquement
.opencode/
AGENTS.md
